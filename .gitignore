# Node
node_modules/

# Databases & logs
*.db
*.db-wal
*.db-shm
*.log

# Local-only operational notes (contains home IPs / usernames)
backend-setup.local.md

# Scraped third-party vocab data (kid-reminder repo is public; this is
# SGSchoolKaki's own curated word bank content — keep it local-only, don't
# republish their content in our public repo)
tools/vocab-scraper/vocab-raw.json

# Personal mistake-log data pulled from the kid's own private repo — keep local,
# don't publish a real child's schoolwork/mistakes in this public repo
tools/english-wrong-answers/parsed-questions.json
tools/english-wrong-answers/unparsed-questions.json
tools/english-wrong-answers/wrong-answers.md

# PSLE Science prelim papers + everything derived from them. These are other
# schools' copyrighted exam papers, downloaded for one child's personal study —
# free to use that way, NOT ours to republish in a public repo. Commit the
# tooling in tools/science-oeq/, never the content.
tools/science-oeq/papers/
tools/science-oeq/survey/
tools/science-oeq/*-questions.json
tools/science-oeq/survey-report.json
backend/science-images/

# Generated TTS audio cache (dictation/English), regenerated on demand
backend/dictation-audio/
backend/english-audio/

# macOS
.DS_Store

# Swift build output (macOS app)
.build/
build/
*.xcodeproj/xcuserdata/
*.zip
